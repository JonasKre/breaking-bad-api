import React from 'react';
import '../App.scss';

function Header() {
    return(
        <header className="app-header">
            <img src="images/logo.svg" alt="Breaking Bad Logo"/>
        </header>
    );
}

export default Header;